# Base de datos PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=expertos
DB_USER=postgres              # En producción (Render): usar "diaguser"
DB_PASSWORD=password          # En producción: usar password de Render

# JWT
JWT_SECRET=tu_clave_secreta_muy_segura_aqui
JWT_EXPIRES=7d

# Node Environment
NODE_ENV=development

# Puerto del servidor
PORT=4000

# CORS
CORS_ORIGIN=http://localhost:5500,http://127.0.0.1:5500
